import styled from 'styled-components';

import { system } from 'ui-kit';

const GroupSingle = {};

const ChatContainer = styled.div`
  position: relative;
  height: 512px;

  ${system}
`;

GroupSingle.ChatContainer = ChatContainer;

export default GroupSingle;
