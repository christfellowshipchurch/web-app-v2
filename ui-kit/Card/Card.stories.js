import React from 'react';

import Card from './';

export default {
  component: Card,
  tags: ['autodocs'],
};

export const Default = () => (
  <Card>
    <h1>Shared Card Wrapper Component</h1>
  </Card>
);
